<Window x:Class="LibraFlow.Views.AddBookDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Style="{StaticResource MaterialDesignWindow}"
        Title="Add New Book" Height="300" Width="450" WindowStartupLocation="CenterOwner">
    <StackPanel Margin="20">
        <TextBlock Text="Title" />
        <TextBox x:Name="TitleBox" Margin="0,0,0,10" Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}"/>
        <TextBlock Text="Author" />
        <TextBox x:Name="AuthorBox" Margin="0,0,0,10" Text="{Binding Author, UpdateSourceTrigger=PropertyChanged}"/>
        <TextBlock Text="ISBN" />
        <TextBox x:Name="ISBNBox" Text="{Binding ISBN, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Width="75" Margin="0,0,10,0" Click="Ok_Click"/>
            <Button Content="Cancel" Width="75" Click="Cancel_Click"/>
        </StackPanel>
    </StackPanel>
</Window>
