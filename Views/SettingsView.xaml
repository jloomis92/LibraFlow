<UserControl x:Class="LibraFlow.Views.SettingsView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:LibraFlow.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">

    <Grid>
        <StackPanel Margin="20">
            <TextBlock Text="LibraFlow Settings"
                       FontWeight="Bold"
                       FontSize="16"
                       Style="{DynamicResource MaterialDesignHeadline6TextBlock}" />
            <StackPanel Orientation="Horizontal" Margin="0,20,0,0">
                <TextBlock Text="Theme:" VerticalAlignment="Center"
                           Style="{DynamicResource MaterialDesignBody1TextBlock}" FontWeight="Bold" />
                <ComboBox x:Name="ThemeComboBox"
                          Width="100"
                          Margin="10,0,0,0"
                          SelectionChanged="ThemeComboBox_SelectionChanged"
                          Style="{DynamicResource MaterialDesignComboBox}"
                          materialDesign:ColorZoneAssist.Foreground="{DynamicResource MaterialDesignBody}"
                          FontFamily="{materialDesign:MaterialDesignFont}">
                    <ComboBoxItem Content="Light"/>
                    <ComboBoxItem Content="Dark"/>
                </ComboBox>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
